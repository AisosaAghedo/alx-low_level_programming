#include "main.h"
#include <ctype.h>
#include <string.h>
#include <stdio.h>

/**
 * *string_toupper - function that changes all lowercase letters to uppercase
 * @str: the string
 * Return: Always 0
 */
char *string_toupper(char *str)
{
	return (toupper(str));
}
