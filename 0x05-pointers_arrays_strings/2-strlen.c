#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - function to print the length of a string
 * @s: string to check the length
 * Return: 0
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
