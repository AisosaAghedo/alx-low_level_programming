#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - function to print the length of a string
 * @s: string to check the length
 * Return: 0
 */
int _strlen(char *s)
{
	printf("%lu\n", strlen(s));
	return (0);
}
